# WSOP 2026 Poker Tournament Scheduler

## Recent Changes
<!-- Update this section at the end of each work session so the next instance knows where things stand. Most recent first. -->
<!-- RULE: Before committing, always update this section with a summary of what changed in this session. -->
- **2026-06-30 (MIXED-GAMES TRAINER — full 5-game suite, play-out + range-aware EV-loss grading)**: Built the **Trainer** (admin Hands tab → "Trainer", `vite-app/src/components/RazzTrainerView.jsx` + backend `server.js:7010-7760` + `solver/razz-trainer/` (stud) and `solver/draw-trainer/` (draw)). You play full heads-up hands vs the blueprint bot and get **per-street EV-loss grading** after the hand plays out. **Games (all live + verified): Razz · Stud 8 · 2-7 TD · Badugi · A-5 TD.** Routes `POST /api/solver/trainer/:game/(deal|step)`; blueprints cached per-game by `getTrainerBp` (restart Express to load a fresher blueprint). **Grading architecture (`gradeHand`):** blueprint-as-oracle; the opponent is a **reach-weighted distribution over hidden hands** (NOT the revealed cards → clairvoyant-free); EV-loss is a **CRN-paired** MC difference (common random numbers across actions → low variance); **exact-forward enumeration** when no chance remains (stud 7th / post-last-draw → SE≈0). Stud games (razz/stud8) share `razz-trainer/` (upcards + 7th-street hole-pair/river reach decomposition); draw games (td27/badugi/a5td) share `draw-trainer/` which **reuses the `lbr-draw.js` particle machinery** (a draw node and a bet node are the same to the grader — `game.legalActions` returns f/c/r or d0..dK). The whole stack is **game-parameterized** (a `game` param); a5td needed ZERO game-specific trainer code. **Features:** full discard control (click cards to KEEP/THROW, explicit `d:`-encoded discard graded identically to the abstraction's recommendation, off-book counts flagged low-confidence); stud **dead cards** from folded opponents (seeded folded door cards drawn from an unreachable deck region, conditioning BOTH the opponent range and the rollout deck); per-game localStorage scoreboard. **TRUST (LBR/fixed-exploiter, chips/hand, see `solver/strategies/BLUEPRINT_TRUST.md`):** razz **0.0**, **stud8 23.4 (BAD — over-folds to aggression, undertrained at 1.9 iters/infoset)**, td27 **2.84**, badugi **0.0**, a5td **0.0**. Surfaced as **in-app trust badges** (✓ trustworthy / ⚠ approximate-grades warning for stud8). **stud8 is being retrained FROM SCRATCH** (the 431k average's checkpoint was lost; only an orphaned Jun-18 668k-infoset experiment ckpt remained → quarantined as `stud8.ckpt.stale-jun18.json`; frozen average backed up to `stud8.frozen-431k.json`) → `stud8.retrain.json` at 5M iters with the validated 230k abstraction; live `stud8.json` stays frozen until the retrain beats 23.4. **a5td abstraction myth busted:** its 4.3x infoset count vs td27 (583k vs 136k) is **reach-path multiplicity, NOT bucket granularity** (72 buckets vs 61 — only 18% more); the ace flag is the legit analogue of 2-7's deuce flag — do NOT coarsen it; a5td is already trustworthy (0.0) at 2.5M iters. **Grade-variance fix (`draw-trainer/grade.js`):** added adaptive `targetSE` sampling — independent CRN rollout **repeats per particle** (`evRepeats` 1→16, NOT a bigger particle population, which would disturb the exact-forward anchor) until paired evLoss SE ≤ 0.3; cut early-draw SE ~3x (td27 0.74→0.21, badugi 0.70→0.26) with exact-forward grades byte-identical, ~150ms/hand; `server.js` passes `targetSE:0.3, maxRepeats:16`. **CAVEATS:** stud8 grades are approximate until the retrain lands (flagged in UI); early-draw grades carry an irreducible range/belief-uncertainty tail (range-dominated, not MC — verified) plus a small **maximization bias** (~1.9 chips, *shrinks* with rollout budget → not a σ leak); stud GTO frequencies don't second-order-condition on dead cards (HU blueprint); a known **`lbr-draw.js` memoizeCfg cache nondeterminism** makes a draw hand's grade depend on batch order (11.98 isolated vs 10.55 after a batch — flagged, orthogonal). Retired the redundant "Watch Solver" admin tab. Gates: td27/badugi grade selftests 12/12, `npm run test:solver` 43/43.
- **2026-06-25 (PARTICLE-FILTER LBR for the draw games — trustworthy exploitability)**: Built the draw-game analogue of the Kuhn-calibrated LBR (`solver/lbr-draw.js` + parallel driver `solver/lbr-draw-run.js`), giving a TIGHT exploitability LOWER BOUND for the **td27** and **badugi** blueprints — the long-queued "LBR meter." Maintains N weighted particles = candidate opponent hands consistent with the public state + my dead cards; on each opponent bet reweights by σ(action) via particle→`cfg.bucket`→infosetKey→σ (verified: after a BB 3-bet the posterior shifts onto deuce-draws), through draws resamples via `cfg.chooseKeep`+observed count from the per-particle unseen pool; at its own betting nodes best-responds by rolling the particles to showdown. **Three things make it trustworthy/tight (each fixing a real failure mode):** (1) continuation = **max over {sigma, aggro}** rollout policies — a naive call-down loses ~2.7 chips/hand to σ in badugi by itself and drove the deviation NEGATIVE; σ-continuation is competent and 'aggro' (keep firing) exposes the over-folding leak a σ-continuation can't; each is a real policy so exploitability ≥ their max. (2) **deviate-only-on-confident-improvement** (`margin`, default 0.15) with **common-random-numbers across actions at a node** — a noisy argmax is not a best response and realised WORSE than σ until gated. (3) ship **max(particle-filter, fixed-exploiter)** — a lower-bound meter publishes the best bound it can prove. CRN deal-pairing + per-hand-deviation **standard errors** reported. Perf: memoized the pure `cfg.bucket/drawOptions/chooseKeep` hot path; parallel driver forks one worker per (seat×mode). **RESULTS (combined meter, shipped):** **td27 ≈ 2.84 chips/hand** (particle-filter finds 1.45 ±0.15 in aggro mode at 120p/6000h, but the simple maniac fixed-exploiter is still tighter at 2.84 — honest BELOW on gate 1, combined ships 2.84); **badugi ≈ 0.0 (≤~0.2) chips/hand** (fixed-exploiter is 0; particle-filter best-seat dev +0.04 ±0.14 at 120p/8000h → indistinguishable from zero → the blueprint is hard to exploit). **SANITY GATES (all reported honestly via `--sanity`):** (1) PF≥fixed — PASS badugi, BELOW td27 (maniac tighter); (2) small-positive — PASS both; (3) broken strategies LARGE — **uniform-random LBR ≈ 6.5–7.3** ≫ blueprint (clean PASS both); always-fold ~1.5 (deviation baseline confounded, shown as context); (4) stable in particle count — PASS both (td27-aggro looked like it drifted down at 2500 hands but that was low-hand noise: N=120 and N=240 both ≈1.44 at 5–6k hands). **VERDICT: TRUST the badugi number (≈0, near noise floor) and the combined td27 number (2.84); the particle-filter alone does NOT yet beat the maniac on td27.** 39/39 solver tests pass (3 new draw-LBR tests). Kuhn LBR (`solver/lbr.js`) untouched and still calibrated. Run: `node solver/lbr-draw-run.js --game td27|badugi --particles 120 --hands 6000`, or `node solver/lbr-draw.js --game badugi --particles 100 --hands 1500 --sanity`.
- **2026-06-20 (first production-grade Stud 8 net)**: Trained + saved the first real Stud 8 7th-street value net — `solver/neural/nets/st7_100k.pt` (6.4 MB), **val R²=0.879, MAE 0.023** on 100k examples (`validate.py --max 100000 --epochs 100 --save --log`, `OMP_NUM_THREADS=1`). Scaling curve now **R² 0.59→0.71→0.77→0.88** (1.5k→12.75k→30k→100k) — steepest gain was 30k→100k, so still data-limited and will exceed 0.9 on the full **1.22M**-example dataset. Grind + caffeinate were cleanly paused for a laptop-lid close and resumed (data intact, verified via `find` — NOT `ls *.jsonl`, ARG_MAX). Razz dataset scaling in progress (`datagen_razz` → `data/razz7`) to train the razz full-game net into `net_leaf`. Remaining queue: razz net, vectorize re-solver, LBR.
- **2026-06-19 (Stud 8 gate RESOLVED + razz product expansion)**: **The scale-vs-EMD gate is decisively answered: DATA-LIMITED → SCALE.** Full scaling curve (val MAE / R²): 1.5k=0.045/0.59 → 6k=0.042/0.67 → 12.75k=0.038/0.71 → 25-30k=**0.034/0.76** (3 independent high-data points consistent), train flat ~0.005-0.007. Monotonic, not decelerating → **25 buckets is NOT the ceiling**; keep the grind running, EMD bucketing deferred. (`solver/neural/data/validation_curve.csv`.) **Op lessons:** run `validate.py` FOREGROUND + `OMP_NUM_THREADS=1` — background val tasks got reaped in idle gaps (the grind only looks immune because `collect-daemon` relaunches dead workers), and multi-thread torch is pure sync overhead on the tiny net (a 6-thread run burned 86 CPU-min on one point); also `caffeinate` had lapsed (machine dozing killed tasks) — restarted. Box is 64 GB RAM (not memory-bound). **Razz is now a confirmed PRODUCT** (user will use it) with two shipped pieces beyond the solver: `npm run razz -- ...` (exact spot solver, `solve_spot.py` parameterized with `--game`) and **razz equity graphs** (`solver/eval/razz.js` JS low eval + `equity.js` razz matchup). `net_leaf.py` parameterized (`bucketing=bucket_razz`) so a trained razz net drops into early-street resolving. **GUI mockup:** `solver/razz-solver-gui.html` — self-contained, real futuregame.me tokens (frontend is now `vite-app/`, not `public/index.html`), real `solve_spot` output, 2 preset spots; open directly (`open solver/razz-solver-gui.html`) — the preview panel 404s it because that routes through the app dev server. Durable overnight worklog: `solver/neural/OVERNIGHT.md`. Remaining queue: train+save a scaled razz net into `net_leaf`; vectorize the re-solver; LBR meter.
- **2026-06-18 (razz neural solver — DeepStack pipeline VALIDATED end-to-end)**: Built a razz neural solver as the cheap end-to-end validation of the Stud 8 stack (razz = the simplest stud instance: ONE strategic dimension, trivial eval). **Result: the stack works.** A razz value net trained on a first 560-example batch hits **val R²=0.895, MAE 0.051 vs 0.194 baseline** (3.8× better than predict-the-mean; train MAE 0.013 → the gap is data-limited overfit, NOT a bucketing ceiling → *scale up*). For contrast Stud 8's first run was R²=0.39 — razz is cleaner because it's 1-D, as predicted. This **de-risks the whole neural direction**: Stud 8 is now an abstraction-scaling problem, not a does-it-work gamble. New modules (all `solver/neural/`, pure-Python, self-tested in `run_tests.sh`): `eval_razz.py` (ace-to-five low), `razz_game.py` (the razz **`GameSpec`**: high-card bring-in, lowest-board-acts-first, whole-pot low showdown — no split/qualifier), `bucket_razz.py` (8-bucket 1-D low ladder, reuses bucket.py's aggregation), `bucket_resolve_razz.py` (bucketed throughput engine, ~0.5s/board), `datagen_razz.py` (same JSONL schema, `game='razz'`, `n_buckets=8`). **`resolve.py` was parameterized** with a `GameSpec` (showdown-share + bring-in + first-actor seams), defaulting to Stud 8 so its tests stay byte-identical; razz reuses the entire CFR+/exact-best-response engine over the *same* betting tree. `validate.py` now auto-detects the bucket count (works for 25-bucket Stud 8 and 8-bucket razz). **The razz re-solver is already a usable EXACT razz solver** for later streets (the analog of `solve_spot` — `resolve_subgame(pbs, game=RAZZ)`). First dataset in `solver/neural/data/razz7`. See `solver/neural/RAZZ.md`. Next: scale razz data (val MAE → train) if desired; the approach is validated.
- **2026-06-18 (solver hardening + tooling)**: **Found & fixed a real bug in the data-parallel CFR trainer** (`solver/engine/parallel.js`). The additive delta-merge (`base += Σ_w(worker−snapshot)`) is incompatible with DCFR's multiplicative regret discount — for `workers>1` it applied the per-iteration decay W times and flipped negative regrets, making strategies *more* exploitable with training (proven on Kuhn exact-BR: W=4 → 0.142 vs single-thread 0.0016; `workers=1` is unaffected, which hid it). This had silently corrupted a *midday parallel retrain* of td27 (exploit_lb ~2.6→~7.9) and badugi; **reverted both draw games to their committed single-thread blueprints**, parked the corrupt 7M/2.8M versions in `solver/strategies/_parked-midday-run/`. **Fix:** `mergeAverage` averages the per-round worker tables (DCFR-correct: discount applied once), Kuhn-verified (W=8 → 0.0008, ≤ single-thread); 32/32 JS tests pass. **Semantic change:** parallelism now buys *variance reduction at the same iteration rate*, NOT W× iterations (so the old parallel iteration counts were also inflated). `PARALLEL.md`/headers corrected; a temporary `train.js` workers>1 guard was added then lifted post-fix. **New `solver/equity.js`** — Monker-style HU equity (hand-vs-range + sorted equity distribution / "equity graph") for all three games, reusing the evaluators, no CFR; self-tested. **Living status board:** `solver/PROGRESS.md` (Mermaid gantt + status table) + pinned standalone `solver/progress-gantt.html`; **multiway** is tracked on the backburner (big lift: stack is 2-player; multiway CFR is heuristic/general-sum). **24/7 stud8 bucketed datagen** is running via `solver/neural/collect-daemon.sh` (self-healing keeper, **8 workers = physical cores** — this is an 8-core/16-thread i9, and >8 workers thrash memory bandwidth) + `monitor.py` live dashboard + `caffeinate` (lid open). ~190k examples; **data validated** (solve exploitability <1% of pot, zero-sum, all buckets covered). Spotlight was excluded from `data/` (it was indexing the many shards). **Next:** LBR/best-response meter (particle-filter for the private draws, Kuhn-calibrated) for trustworthy draw-game exploitability; the parallel fix is done.
- **2026-06-18 (overnight)**: Set up **autonomous overnight progress** — `solver/neural/overnight.sh` (run via nohup): keeps the 3-worker bucketed grind alive AND every 30 min re-trains/validates the net on the accumulated data, appending a **scaling curve** to `solver/neural/data/validation_curve.csv` (timestamp, n_examples, val_mae, train_mae, baseline_mae, r2) and saving `nets/st7_latest.pt`. `validate.py` gained `--log`/`--save`. **Morning read:** `column -s, -t solver/neural/data/validation_curve.csv` — if **val_mae falls toward train_mae** as n_examples grows → the approach works, scale up (more workers/data, maybe more buckets); if **val_mae plateaus well above train_mae** → 25 buckets is the ceiling, build **EMD bucketing (true Milestone B)**. Orchestrator pid in `data/overnight.pid` (stop: `kill $(cat ...)`); it self-exits after ~12h. **Scaling curve trend (encouraging):** val_mae fell 0.045→0.042→0.038 and R² rose 0.59→0.67→0.71 as data grew 1.5k→6k→12.75k — i.e. **data-limited, not bucketing-limited**; more data keeps helping (train_mae stays ~0.004, so headroom remains).
- **2026-06-18 (cores reallocated to datagen)**: Per the user, **stopped all tabular training and gave the box to datagen.** SIGINT'd the supervisor → graceful shutdown checkpointed + stopped td27 (1.88M, ckpt 175MB) and badugi (1.75M, ckpt 24MB) — both **converged/mature**, so frozen with **resumable checkpoints** (relaunch `npm run supervise` to resume; stud8 stays excluded/frozen at the restored 431k average + its 16k from-zero ckpt). Then bumped the grind from 3→**14 workers** (16 cores, leaving 2 for OS + the periodic validation) via `WORKERS=14 bash overnight.sh` — ~5× the datagen throughput. All three games' checkpoints are intact in `solver/strategies/*.ckpt.json`.
- **2026-06-18**: Neural Stud 8 — **validation harness + first measurement (encouraging, data-limited)**. Built `solver/neural/validate.py` (the decision gate): trains a net on datagen shards, reports per-bucket **CFV accuracy** (val/train/baseline MAE + R²) with a verdict that distinguishes *fits-train-but-overfits* (→ data-limited, scale up) from *can't-fit-train* (→ bucketing/capacity-limited, do EMD) — plus an optional, SLOW net-as-leaf 6th-vs-exact fidelity check (the exact 6th solve nests a 7th CFR solve per deal leaf, so keep it tiny). Installed **PyTorch 2.2.2 + numpy 1.26.4** in `solver/neural/.venv` (gitignored; pin numpy<2 — torch 2.2 has a numpy-2 ABI clash). **First real run** (450 bucketed 7th-street examples, 383/67 split, 150 epochs): val MAE **0.055** vs predict-the-mean baseline **0.086** (R²=0.39), train MAE **0.016**. Read: the net fits the targets and beats baseline → **the approach has real signal**; the train↔val gap is overfitting from tiny data → **data-limited, NOT bucketing-limited** (if 25 buckets were the ceiling the net couldn't fit even the train targets). Also confirms the whole torch pipeline (train.py/value_net/net_leaf/featurize) runs end-to-end. **Next:** let the 24/7 grind pile up thousands of examples, re-run `validate.py --shards data/st7`; if val MAE falls toward train → scale; if it plateaus high → EMD bucketing (true Milestone B) is the lever. Run: `.venv/bin/python validate.py --shards data/st7 --epochs 200`. NOTE: tried to stop the mature td27/badugi trainers to free cores for datagen — **correctly blocked by the permission classifier** (uncheckpointed progress risk); left them running, generated the validation set on my own grind cores instead. The grind (3 workers, `data/st7`) is running.
- **2026-06-18**: Neural Stud 8 — **study spot-solver + 24/7 bucketed data grind** (the prioritized "fastest progress" work). All self-tested via `solver/neural/run_tests.sh` (~55s, pure Python). Insight driving it: the re-solver IS a Stud 8 solver for later streets; the net is just an optimization for early streets / real-time speed — so exploit what's built. New/changed:
  - **Sparse-support solve** — `resolve.py` `resolve_subgame(..., holdings=<subset>)`: restrict CFR to the union of both ranges' support. **Proven exact** (self-test: explicit-support solve == full solve restricted to those holdings). Makes narrow / node-locked spots fast (the 6th→7th exact recursion was also made sparse — lifts only reachable 3-card holdings).
  - **`solve_spot.py`** — on-demand EXACT node-locked spot solver (CLI + JSON). Board + each player's range (node-lock the opponent to anything) → equilibrium strategy + action frequencies + per-side EV (chips) + exploitability. The #1 study deliverable; needs no training/torch. e.g. a 7th-street node-locked spot returns hero check 85%/bet 15%, EV +6.7 chips, in <1s.
  - **Bucketed re-solver** (the 24/7 throughput engine) — `resolve.py` gained a `share_matrix=` hook: run CFR over hi×lo buckets instead of raw holdings (no card collisions; a fast factored showdown path). `bucket_resolve.py`: `sample_share_matrix` (seat-0 expected share per bucket pair, sampled, precomputed hi/lo, `holding_cap`; depends only on the board → amortized) + `resolve_bucketed`. A **full 7th board (~13k holdings) solves over 25 buckets in ~3.4s** vs intractable raw.
  - **`datagen_bucketed.py`** — bucketed data generation: per board, compute the share matrix once, then solve many bucketed range samples on it. JSONL records (`bucketed:true`, per-bucket branges/cfv) consumable by `train.py` (its `featurize` now handles both raw and bucketed schemas).
  - **`datagen-24-7.sh`** — parallel continuous runner: one Python process per core (real parallelism), unique shard tags, restart-safe (continues shard numbering). Run in tmux: `STREET=7 OUT=solver/neural/data/st7 bash solver/neural/datagen-24-7.sh`. Verified producing valid shards across 2 workers. **To start the grind the user asked for, run this on the box.** Output feeds `train.py --shards`.
- **2026-06-18**: Neural Stud 8 — **Milestone C data pipeline + B-v0 bucketing** (builds on Milestone A below). New pure-Python modules, all self-tested via `solver/neural/run_tests.sh` (~37s, no numpy/torch): `datagen.py` (sample reachable PBS + ranges → `resolve_subgame` → JSONL CFV shards; `read_shards` reloads; deterministic by seed), `bucket.py` (Milestone B v0: fixed 25 buckets = hi-class(5)×8-or-better-lo-class(5); **range-weighted aggregation preserves the range-weighted value exactly** — tested; `bucket_map`/`aggregate_range`/`aggregate_cfv`/`scatter_cfv` are the stable interface, EMD clustering is the upgrade), `net_leaf.py` (adapts a trained net to resolve.py's `leaf_value_fn` contract: bucket reach → net → ×pot → scatter to holdings; glue tested with a fake predictor + a zero-leaf resolve integration), `train.py` (`featurize` example→tensors-as-lists is pure-Python & tested; the Huber+Adam loop + `torch_predict_fn` import torch lazily so the modules load/test without it). `pbs.py` gained `encode_board`/`encode_pbs` (BOARD_DIM=51, EXTRA_DIM=8) + holding helpers. The full **sample→solve→bucket→CFV-target pipeline runs on CPU**; remaining for Milestone C is running the actual `train.py` fit on a PyTorch box (7th street first, then bootstrap earlier streets with the trained net as `leaf_value_fn`, `depth_limit=1`). See `solver/neural/README.md`.
- **2026-06-18**: Training ops — **stud8 worker stopped + 431k strategy restored** (per user). The supervisor had relaunched all three trainers but stud8's full-state checkpoint was never restored, so it had **restarted from zero** (was ~16k iters, exploit_lb ~22 vs the committed 431k). SIGTERM'd the stud8 worker (PID 22841 → clean checkpoint + exit 0; the supervisor treats code 0 as "done" and does NOT restart, so td27/badugi were untouched) and `git checkout HEAD -- solver/strategies/stud8.json stud8.meta.json` to restore the **431k average**. **The 431k training STATE is unrecoverable** — `train.js` resumes only from `<out>.ckpt.json` (the from-zero run overwrote it; no `.gz` backup exists anywhere). stud8 is now **frozen at the 431k average** pending a user call on freeze-vs-grind-from-16k. The 16k from-zero checkpoint is kept at `stud8.ckpt.json` (a future `supervise` run WOULD auto-resume it and re-overwrite the average — run `supervise --games td27,badugi` to keep stud8 frozen). **Heads-up:** td27/badugi have **no on-disk checkpoint** (parallel path only checkpoints on a merge, ~every 700k iters) — a crash could lose progress past 1.88M/1.75M; a clean supervisor restart would force checkpoints.
- **2026-06-18**: Neural Stud 8 solver — **Milestone A done** (`solver/neural/resolve.py`). Implemented the range-form **CFR+ subgame re-solver**: given a PBS (public board + both players' ranges over hidden holdings) it builds the current street's public betting tree (a faithful pure-Python port of `games/stud8-game.js` — bring-in/complete, best-board-acts-first, small/big bets, 4-bet cap, fold/showdown) and returns the average strategy + **per-holding counterfactual values** (the value-net training target). 7th street solves exactly (hi/lo showdown via `eval_stud8.py`); street boundaries are valued by a `leaf_value_fn` (the net — Milestone D) when depth-limited; 6th street does an exact one-level recursion (the 7th card is down, so one shared lifted 7th-street solve — no public-card blowup); 3rd–5th without a net leaf raise a deliberate `NotImplementedError` (public-upcard enumeration is intractable — that's what the net is for). Includes an **exact best-response exploitability gauge** (the repo's "measure before you trust" gold standard, here exact for the 7th-street subgame). Also implemented `pbs.py` `encode_board`/`encode_pbs` (BOARD_DIM=51, fraction-of-pot extras) + holding helpers. All pure Python (no numpy/torch) so datagen runs on any CPU box. **Validated:** zero-sum to float precision, best-response exploitability →0, dominance value ≈ half-pot to a scooper, exact-recursion + error-guard tests (`python3 resolve.py`, ~15s), plus a **7000-case cross-check** of the Python evaluator + first-to-act against the JS engine (0 mismatches). Next: Milestone C (`datagen.py`/`train.py` → fit `value_net.py`, 7th-street first then bootstrap) and Milestone B (EMD/OCHS holding bucketing to scale past ~15k raw 7th-street holdings). See `solver/neural/README.md`.
- **2026-06-18**: Local continuous-training stack + handoff — built the system to grind the CFR solvers 24/7 on a dedicated machine (training is pure local compute, zero API calls). New: `solver/engine/parallel.js` + `cfr-worker.js` (data-parallel MCCFR via worker_threads; additive delta-merge; verified `workers=1` == single-thread byte-for-byte; `--worker-heap` raises per-worker V8 caps since `--max-old-space-size` doesn't cover worker threads). `solver/supervise.js` (self-managing supervisor: runs all three games concurrently, auto-resumes from checkpoints across crashes/OOM with backoff, meters exploitability on a schedule, logs the curve to `strategies/curve.csv`, graceful SIGINT checkpoint-all). `solver/analyst.js` (read-only report: convergence + recommended `--workers` allocation, plain-English strategy heuristics decoded from each game's exact infoset abstraction, optional `--narrate` Claude commentary — the only thing here that calls any API). `solver/setup-local.sh` (one-command machine setup). `train.js` gained `--workers`/`--merge-every`/`--worker-heap`/`--ckpt-every` (atomic periodic checkpoints) + SIGTERM/SIGINT checkpoint-on-exit (single-thread loop yields to honor signals). `exploitability.js` gained `--file`. npm scripts: `supervise`, `analyst`. Docs: **`solver/HANDOFF.md`** (start here in a fresh/local session) + `solver/PARALLEL.md` (operator guide). Note: CFR checkpoints (`strategies/*.ckpt.json`) and `curve.csv` are gitignored; a fresh clone has pruned strategies but resumes from zero unless the checkpoints are restored.
- **2026-06-14**: Solver self-play viewer — new "Watch Solver" tool on the admin Hands tab (third toggle alongside Replayer / Solver Trainer). `GET /api/solver/playout/:gameId` plays a full hand with both seats sampling the trained strategy (`solver/playout.js`) and returns the trajectory; `SolverPlayView.jsx` steps through every decision (both hands face-up, acting seat highlighted, mixed-strategy frequency bars with the sampled action marked, prev/next/autoplay) and shows a hi/lo split-aware showdown. Added `viewAll`/`result` to each game and `describeHi7`/`describeLo8` to the stud8 evaluator. Shared `SolverCard.jsx` extracted from the trainer. Uses existing futurega.me theme tokens.
- **2026-06-10**: CFR solvers + trainer for mixed games — new `solver/` module with external-sampling MCCFR engine (regret-matching+, linear averaging), hand evaluators and heads-up fixed-limit game implementations for **2-7 Triple Draw**, **Badugi**, and **Stud 8 or Better**. Card-bucket + street-local-history abstraction keeps infoset spaces tractable; draw nodes offer only snow/natural/break counts. Training CLI `npm run train -- --game td27 --iters N` with checkpoint/resume (`--minutes` time box); strategies saved to `solver/strategies/*.json` (checkpoints gitignored). Engine validated by Kuhn-poker convergence test (`npm run test:solver`, 24 tests). API: `GET /api/solver/games`, `GET /api/solver/spot/:gameId` (quiz spot dealt by self-play from the trained strategy). Frontend: `SolverTrainerView.jsx` quiz UI behind a Replayer/Solver Trainer toggle on the admin-gated Hands tab. See `solver/README.md`.
- **2026-05-18**: Schedule tab performance + Vite dev server fix (IN PROGRESS — NOT YET VERIFIED)
  - **CalendarEventRowLite**: New zero-hook pure function component (`vite-app/src/components/CalendarEventRow.jsx`) that renders only the collapsed bar. Eliminates ~15 hook calls per row. Exported as named export `{ CalendarEventRowLite }`.
  - **TournamentsView.jsx** (the actual "Schedule" tab — `id:'tournaments'` in BottomNav): Now uses CalendarEventRowLite for non-expanded, non-today rows. `activatedIds` state + `activateRow` callback swap in full CalendarEventRow on tap. Added `contentVisibility:'auto'` wrappers.
  - **ScheduleView.jsx** ("My Schedule" view): Same CalendarEventRowLite pattern applied. Reduced `visibleCount` 10→3, batch 20→10. Fixed dead `setListVisible` calls. Changed scroll-to-today from useLayoutEffect→useEffect.
  - **CalendarEventRow.jsx**: Added `initialOpen` prop so rows open immediately when swapped from Lite→Full.
  - **Vite CJS/ESM fix**: Changed `import ReactDOM from 'react-dom'` → `import { createPortal } from 'react-dom'` and `ReactDOM.createPortal` → `createPortal` in 17 files (App.jsx, ToastContext.jsx, ShareMenu.jsx, WrapUpViewer.jsx, NotificationsPanel.jsx, TournamentsView.jsx, CalendarEventRow.jsx, SwapModal.jsx, MilestoneCelebration.jsx, RealNamePrompt.jsx, HandReplayerView.jsx, SocialView.jsx, StakingView.jsx, ScheduleExportModal.jsx, FilterPanel.jsx, CalendarView.jsx, LiveUpdatePanel.jsx). This fixes "Invalid hook call" blank screen in Vite dev mode caused by duplicate React instances from CJS default export interop.
  - **CORS**: Added `http://localhost:5173` to `ALLOWED_ORIGINS` in `server.js` so Vite dev server can talk to the API.
  - **STATUS**: Both servers run (`node server.js` on 3001, `npx vite` on 5173). CORS log gone. But **nothing has been visually verified yet** — need to open http://localhost:5173 in a browser and confirm (1) app renders (no blank screen), (2) Schedule tab loads fast. Then rebuild + deploy to TestFlight to verify on iOS device.
  - **IMPORTANT tab mapping**: BottomNav label "Schedule" → `id:'tournaments'` → renders `TournamentsView`. "My Schedule" is `ScheduleView`, accessed differently. The TournamentsView is the one that was slow (hundreds of tournament rows).
- **2026-04-20**: Multi-day event restart audit — added missing Day 2 cards for WSOPC Cherokee (Mini Main, Monster Stack, NLH, Main Event), WSOPC Horseshoe Las Vegas (Mini Main, Monster Stack, Main Event), Turning Stone Casino (Mini Main, Monster Stack, Main Event). Renamed Venetian "NLH MSPT - Day 2" (Jul 19) → "NLH MSPT Heart Poker Championship - Day 2". Fixed orphaned Orleans "NLH Mega Stack - Flight B" → "NLH Mega Stack". Script: `add-day2-restarts.js`.
- **2026-04-08**: Deploy script with DB sync — `deploy.sh` builds, pushes to git, waits for Render deploy, then syncs local tournament DB and venue colors to production via `/api/tournaments/sync` and `/api/venue-colors`. Admin color picker added to CalendarEventRow edit panel — saves to `venue_colors` table, updates CSS variables for all venues sharing the same abbreviation.
- **2026-04-02**: URL-based schedule import — new `/api/parse-schedule-url` endpoint fetches PDFs, images, or HTML pages from URLs and runs through AI extraction pipeline. Shared parser helpers extracted (getSchedulePrompts, repairTruncatedJsonArray, extractJsonArray, runTwoPassExtraction, postProcessEvents). Fixed imported events not appearing by normalizing human-readable dates to ISO format. Frontend URL input with Fetch button and progress bar.
- **2026-04-02**: Schedule parser improvements — two-pass AI extraction (Sonnet vision → Haiku structuring), assistant prefill to prevent code fences, character-level JSON repair, asymptotic progress bar, scan editor checkboxes (satellite/restart/category), per-day event counts in date headers, Import Schedule button in filters bar.
- **2026-03-08**: User search in Social tab — `GET /api/users/search?q=` endpoint with prefix matching on username/real_name, excludes self and existing connections. Debounced auto-suggest dropdown in SocialView with Connect button.
- **2026-03-08**: Hand Replayer activated — wired up `HandReplayerView` component (was "Coming Soon" placeholder). Custom game type support, manual winner selection, split-pot seat highlighting.
- **2026-03-08**: PDF download fix — `buyinRight` was NaN because `doc.lastAutoTable.columns[4].x` doesn't exist in jspdf-autotable 3.8.4. Fixed by summing column widths from left margin. Added try/catch to `handlePDF` with error alert.
- **2026-03-07**: Hand Replayer companion tool — Canvas-based felt table, flexible game engine supporting any card game (including custom/made-up games), 6 presets (NLHE, PLO, Stud, Draw, Pineapple, Short Deck), game builder UI, hand editor with card picker, step-through replay controls with keyboard support (arrow keys), action log sidebar. New file: `public/replayer.html`. New API routes: `/api/replayer/hands`, `/api/replayer/games`. New DB table: `replayer_games`. Page served at `/replayer`.
- **2026-03-07**: Light mode export option — added "Export in Light Mode" checkbox to schedule export modal. Both PDF and image exports support `opts.light`. PDF uses theme constants (BG, BG_ALT, TEXT_PRI, TEXT_MUT, TEXT_ACC, LINE_CLR, LINE_HEAD, LINE_FOOT) and light venue colors. Image export (`drawSchedulePage`) uses equivalent canvas color variables. `generateScheduleImages` passes opts through.
- **2026-03-07**: PDF total buy-ins right-aligned with buy-in column — used `doc.lastAutoTable.columns[4]` to get the exact buy-in column position and align the total amount with `{ align: 'right' }`. Merged with existing page-overflow logic.
- **2026-03-05**: Spacing below search bar row — added 8px margin-bottom to search bar + filter button row for breathing room above the checkbox pills.
- **2026-03-05**: Vertically center filter button and search bar — overrode `.search-bar` `margin-bottom:10px` with `marginBottom:0` when in flex row with filter button.
- **2026-03-05**: Date range filters game variants — Variant dropdown now only shows game variants that have events within the selected date range (same behavior as venues/series). Uses `availableGameVariants` memo computed from tournaments filtered by `dateFrom`/`dateTo`.
- **2026-03-05**: Added Mixed quick-filter pill — shows events where `game_variant` is neither NLH nor PLO. Filter logic uses OR when multiple pills active (NLH + Mixed shows both). Added `mixedOnly` filter state flag.
- **2026-03-05**: Filter pills full width fix — added `gridColumn:'1 / -1'` to pills container so it spans all columns of the filter panel grid.
- **2026-03-05**: Multi-select filter pills — all five pills (NLH, PLO, Mixed, Ladies, Seniors) toggle independently. NLH/PLO add/remove from `selectedGames` array. Ladies/Seniors use OR logic when both active. Checkboxes in Special dropdown synced with pills (no more clearing other filters).
- **2026-03-05**: Ladies/Seniors checkboxes synced with pills — Special dropdown checkboxes now toggle `ladiesOnly`/`seniorsOnly` (same state as pills). Removed `hideWomens`/`hideSeniors` entirely — these filters only show events exclusively, never hide them.
- **2026-03-05**: Remove connection from Social tab — expanded buddy cards now show "Remove Connection" button with confirmation step. Uses existing `DELETE /api/share-buddy/:userId` endpoint. Added `confirmRemoveId` state and `onRemoveBuddy` prop to SocialView.
- **2026-03-05**: TCH re-entry set to Unlimited — all Texas Card House events had null re-entry. Updated `tch-events.json` seed file and added `tch-reentry-unlimited-2026-03` data migration for existing databases.
- **2026-03-05**: Connection schedule CSS grid alignment — replaced per-row flex layout with single CSS grid (`gridTemplateColumns:'auto auto auto 1fr auto'`) so all rows share column widths. Fixes misalignment between rows with different content lengths (e.g. "Turning Stone" vs "IPO"). Uses `React.Fragment` wrappers so cells participate directly in parent grid.
- **2026-03-05**: Fixed nextThemeLabel crash — `nextThemeLabel` was used in AuthScreen, ForgotPasswordForm, ResetPasswordForm but only defined inside App scope. Fixed by computing locally in each component.
- **2026-03-05**: Filter panel restructuring — renamed "How Much?" to "Buy-in / Rake", added "All" checkboxes to Buy-in and Rake, renamed "Which?" to "Variant", created Special dropdown (Ladies, Seniors, Bounty, Satellites), moved filter button to left of search bar, added quick-filter pills (NLH, PLO, Mixed, Ladies, Seniors) at top of filter panel.
- **2026-03-05**: Moved date range slider outside When? dropdown — slider is now its own top-level filter group in the filter panel, always visible when panel is open (no need to expand When? first).
- **2026-03-04**: Real name support — `real_name` column, `PUT /api/profile`, Full Name on registration, prompt for existing users on login, display name toggle (Real/Username) in Settings via `DisplayNameContext`, all name display locations updated.
- **2026-03-04**: Add connections to groups from Social view — "Add to Group" button on expanded connection cards, new `GET /api/groups/:id/members` endpoint, fixed invite filter to exclude existing members, proper member list in group Members tab.
- **2026-03-04**: Show connection schedules when offline — expanding a connection in Social view now fetches and displays their upcoming schedule (grouped by date), regardless of online status. Schedule data cached after first load.
- **2026-03-04**: User dropdown menu — username/avatar chip opens portal dropdown with "My Schedule" and "Sign Out". Uses z-index 9998/9999 to layer above all other UI. Background uses `var(--surface)` for opacity.
- **2026-03-04**: Guest banner "Register" button links directly to registration form via `initialRegister` prop on AuthScreen.
- **2026-03-04**: Removed global rate limiter (200 req/15min was too aggressive for SPA). Kept auth-specific (10/15min), admin, and staking limiters.
- **2026-03-04**: Guest login expanded — guests can now add events to schedule temporarily (schedule endpoints open to guests). Banner: "Guest mode — your schedule won't be saved. Register to keep it!"
- **2026-03-04**: Added guest login — browse-only access without registration. `POST /api/guest-login` issues a 4h JWT with `isGuest: true`. `requireRegistered` middleware blocks all write endpoints for guests. Frontend: "Continue as Guest" button on AuthScreen, guest banner with sign-up prompt, hidden schedule action buttons.
- **2026-03-04**: Added forgot password feature — token-based reset flow with Nodemailer SMTP support. New endpoints: `/api/forgot-password`, `/api/reset-password`. New `password_resets` table. Frontend: ForgotPasswordForm, ResetPasswordForm components. Reset links use URL hash (`/#reset?token=<hex>`).
- **2026-02-26**: Cloned repo to new machine, set up dev environment (Xcode CLI tools, Homebrew, Node.js). No code changes yet.

## Quick Start
```bash
npm install
npm run init-db        # Creates poker-tournaments.db with schema + sample data
JWT_SECRET="dev-secret-not-for-production" node server.js
```
Server runs on port 3001 (or `PORT` env var). `JWT_SECRET` env var is **required** — server exits without it.

### Optional env vars for password reset emails
| Variable | Purpose | Example |
|---|---|---|
| `SMTP_HOST` | SMTP server hostname | `smtp.gmail.com` |
| `SMTP_PORT` | SMTP port (default 587) | `587` |
| `SMTP_USER` | SMTP username | `noreply@yourdomain.com` |
| `SMTP_PASS` | SMTP password/app password | `xxxx-xxxx-xxxx-xxxx` |
| `SMTP_FROM` | From address | `"Futuregame <noreply@yourdomain.com>"` |
| `APP_URL` | Public base URL for reset links | `https://futuregame.com` |

If SMTP is not configured, reset links are logged to the server console.

## Architecture
- **Frontend**: Single-file React app at `public/index.html` (React 18 + Babel standalone, no build step)
- **Backend**: `server.js` — Express 5 + sql.js (in-memory SQLite loaded from `poker-tournaments.db`)
- **Parsers**: `parsers/wsop-parser.js`, `parsers/generic-parser.js` — PDF schedule importers
- **Database**: `poker-tournaments.db` — SQLite file, gitignored. Run `npm run init-db` to create/reset

## Key Files
| File | Purpose |
|------|---------|
| `public/index.html` | Entire frontend (~17000+ lines of React components, styles, game logic) |
| `server.js` | Express API server, auth, CRUD, live updates, file uploads |
| `init-db.js` | Database schema creation + optional seed data |
| `sample-data.js` | Tournament seed data used by init-db |
| `public/replayer.html` | Hand replayer — Canvas table, game engine, editor, game builder |
| `parsers/` | Schedule PDF parsing utilities |

## Conventions
- All frontend code lives in `public/index.html` — no JSX files, no bundler
- Dark/light theme support via CSS custom properties and `[data-theme]` selectors
- State management uses React hooks (useState, useMemo, useEffect, useCallback)
- Card notation: `AhKs` = Ace of hearts, King of spades. Suits: h/d/c/s, x = face-down
- "Opponent" (not "villain") for non-hero hands — renamed globally
- Hand evaluation supports multiple opponents (array of results)
- Canvas overlays for camera/share images drawn via 2D API
- Dropdowns/panels use `ReactDOM.createPortal` to escape stacking contexts
- Filter panel and live-update panel both portal backdrop + panel to `document.body`

## API
- Auth: POST `/api/register`, POST `/api/login`, POST `/api/guest-login` (JWT-based)
- Password Reset: POST `/api/forgot-password`, POST `/api/reset-password`
- Profile: PUT `/api/profile` (update real_name)
- Tournaments: GET `/api/tournaments`, GET `/api/tournaments/:id`
- Schedule: GET/POST/DELETE `/api/schedule` (per-user saved tournaments)
- Tracking: GET/POST/PUT/DELETE `/api/tracking` (buy-ins, results, P&L)
- Live Updates: GET/POST `/api/live-update`, GET `/api/live-update/active`
- Shared: GET/POST `/api/shared-schedule`
- Share Buddies: GET `/api/share-buddies`, PUT `/api/share-request/:id/accept`, DELETE `/api/share-buddy/:userId`
- Groups: GET/POST `/api/groups`, DELETE `/api/groups/:id`, GET `/api/groups/:id/members`, POST `/api/groups/:id/members`, GET `/api/groups/:id/feed`, POST `/api/groups/:id/messages`, GET `/api/groups/:id/schedule`, GET/PUT `/api/groups/:id/leaderboard`
- Group Invites: GET `/api/groups/:id/invites`, PUT `/api/group-invites/:id/accept`, PUT `/api/group-invites/:id/decline`
- Hand Replayer: GET/POST `/api/replayer/hands`, PUT/DELETE `/api/replayer/hands/:id`, GET/POST/DELETE `/api/replayer/games`
- Schedule Docs: GET `/api/schedule-docs` (list all), GET `/api/schedule-docs/:venue/:type/:filename` (serve file)
- Structure Parsing: POST `/api/parse-structure` (upload structure sheets → AI extraction → update tournament records)

## Schedule Documents
Source schedules and structure sheets are archived in `schedule-docs/<Venue>/schedules/` and `schedule-docs/<Venue>/structures/`. Files are auto-archived when users import via `/api/upload-schedule`, `/api/parse-schedule`, or `/api/parse-structure`. Structure sheets update existing tournament records with granular data (starting chips, level durations, blind levels, re-entry, late reg, day length).

## Dev Server Config
The `.claude/` directory is gitignored. For Claude Code preview tools, create `.claude/launch.json`:
```json
{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev",
      "runtimeExecutable": "node",
      "runtimeArgs": ["server.js"],
      "port": 3001,
      "env": {
        "JWT_SECRET": "dev-secret-not-for-production"
      }
    }
  ]
}
```

## Deployment (Render)
- **Service**: futuregame (`srv-d6b8ujfgi27c73d5v3p0`)
- **Dashboard**: https://dashboard.render.com/web/srv-d6b8ujfgi27c73d5v3p0
- **Deploy**: `./deploy.sh` — builds, pushes to master, waits for Render deploy, syncs local DB → production (tournaments + venue colors). Prompts for admin credentials (or set `ADMIN_USER`/`ADMIN_PASS` env vars).
- **Auto-deploy**: pushes to `master` branch trigger deploy
- **Render config** stored at `.claude/projects/-Users-ethanibennett-WSOP-scheduler/render.env`
- **Fetch logs**: `curl -s -H "Authorization: Bearer $RENDER_API_KEY" "https://api.render.com/v1/logs?ownerId=tea-d6b8t3ali9vc73dcs4q0&resource=srv-d6b8ujfgi27c73d5v3p0&limit=100&direction=backward" | python3 -m json.tool`
- **Filter logs**: add `&text=ParseSchedule` (or any keyword) to filter
- **Render CLI**: `brew install render-oss/render/render` — installed, use `RENDER_API_KEY` env var for auth

## Database
- Schema created by `init-db.js` — tables: users, tournaments, user_schedules, tracking, live_updates, shared_schedules, satellites, password_resets
- `npm run seed` populates with sample WSOP tournament data
- sql.js loads entire DB into memory on startup, persists to disk on writes
